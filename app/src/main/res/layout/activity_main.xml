<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/main_background">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/main_theme"
        android:id="@+id/imageView"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
    <Button
        android:id="@+id/camera_mode"
        android:layout_width="150dp"
        android:layout_height="150dp"
        style="?android:attr/borderlessButtonStyle"
        android:background="@drawable/camera_mode_button_custom"
        android:layout_alignTop="@+id/imageView"
        android:layout_centerHorizontal="true" />


    <TextView
        android:text="SELECIONE O MODO DE FUNCIONAMENTO"
        android:textStyle="bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:gravity="center"
        android:id="@+id/textView"
        android:layout_below="@+id/camera_mode"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="39dp" />

    <Button
        android:id="@+id/remote_mode"
        android:layout_width="150dp"
        android:layout_height="150dp"
        style="?android:attr/borderlessButtonStyle"
        android:background="@drawable/remote_mode_button_custom"
        android:layout_alignBottom="@+id/imageView"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
